#include "cast_spell_command.h"
#include "game.h"

void CastSpellCommand::execute(Game& game) {
    // Spell casting is handled by the game input handler
    // This command just marks that a spell should be cast
}
