#include "GameController.h"
#include <iostream>
#include <cstdlib>
#include <ctime>

/**
 * @mainfile Главный файл игры 
 * 
 * Описание игры:
 * Пошаговая тактическая RPG с элементами стратегии.
 * Игрок управляет героем, который сражается с врагами, разрушает здания и башни
 * на procedurally-генерируемых уровнях с прогрессирующей сложностью.
 * 
 * Особенности:
 * - Два режима боя: ближний (меч) и дальний (лук)
 * - Система заклинаний с ограниченной рукой
 * - Разные типы врагов: обычные, здания-спаунеры, магические башни
 * - Тактические клетки: стены, замедление, ловушки
 * - Прогрессия сложности: FOREST → CASTLE → DUNGEON
 * - Система улучшений персонажа
 * 
 * Управление:
 * - Движение: W-A-S-D
 * - Атака: F
 * - Смена оружия: M
 * - Заклинания: 1-2-3
 * - Показать заклинания: I
 * - Выход: Q
 * 
 * Автор: ЯЯЯЯЗЬ
 * Версия: 1.0
 */

int main() {
    // Инициализация генератора случайных чисел
    std::srand(static_cast<unsigned int>(std::time(nullptr)));
    
    std::cout << "================================================" << std::endl;
    std::cout << "         SUPERPUPERMEGAIGRA" << std::endl;
    std::cout << "================================================" << std::endl;
    std::cout << std::endl;
    
    try {
        // Создание и запуск игрового контроллера
        GameController gameController;
        gameController.run();
        
    } catch (const std::exception& e) {
        // Обработка критических ошибок
        std::cerr << "================================================" << std::endl;
        std::cerr << "КРИТИЧЕСКАЯ ОШИБКА!" << std::endl;
        std::cerr << "Игра завершена из-за непредвиденной ошибки:" << std::endl;
        std::cerr << e.what() << std::endl;
        std::cerr << "================================================" << std::endl;
        
        std::cout << "Нажмите Enter для выхода...";
        std::cin.get();
        return 1;
        
    } catch (...) {
        // Обработка неизвестных ошибок
        std::cerr << "================================================" << std::endl;
        std::cerr << "НЕИЗВЕСТНАЯ КРИТИЧЕСКАЯ ОШИБКА!" << std::endl;
        std::cerr << "Игра завершена из-за непредвиденной ошибки." << std::endl;
        std::cerr << "================================================" << std::endl;
        
        std::cout << "Нажмите Enter для выхода...";
        std::cin.get();
        return 1;
    }
    
    std::cout << "================================================" << std::endl;
    std::cout << "         ИГРА ЗАВЕРШЕНА" << std::endl;
    std::cout << "         Спасибо за игру!" << std::endl;
    std::cout << "================================================" << std::endl;
    
    return 0;
}