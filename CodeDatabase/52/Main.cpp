#include "Application.h"

int main() {
    Application app;   // Создаём объект верхнего уровня
    app.Run();         // Запускаем приложение
    return 0;          // Никакой логики здесь больше нет
}