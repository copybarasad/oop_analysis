#ifndef ENUMS_H
#define ENUMS_H

enum class Direction { UP, DOWN, LEFT, RIGHT };
enum class AttackType { MELEE, RANGED };
enum class GameStatus { RUNNING, LEVEL_COMPLETED, PLAYER_DIED };

#endif